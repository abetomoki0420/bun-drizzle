# bun-drizzle-demo

To install dependencies:

```bash
bun install
```

To create SQLite file:

```bash
touch sqlite.db
```

To execute migration:
```bash
bun run src/db/migrate.ts
```

To run:

```bash
bun run main.ts
```

